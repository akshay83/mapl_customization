/*$('body').on('DOMNodeInserted', '.file-upload', function () {
	if ($(".file-upload").children().has("#scan-btn").length<=0) {
		$(".input-upload").append('<button class="btn btn-primary btn-sm" id="scan-btn">Scan</button>');
		$('.input-upload-file.hidden').attr("capture", "camera");
	}
});*/
